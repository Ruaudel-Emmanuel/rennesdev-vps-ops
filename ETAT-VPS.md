# 📋 État du VPS rennesdev.fr — État courant
> Journal allégé depuis le 2026-09-14 (rotation, règle n°7) : ce fichier décrit l'état **actuel** uniquement.
> Historique complet : `ETAT-VPS-archive-2026-09.md` (et suivants, un par mois) — aussi sur GitHub.
> Rotation : le 1er de chaque mois, les entrées du mois écoulé partent dans une nouvelle archive.

---

## 🆕 Dernière session — 2026-09-17 (3e partie)
- **Récepteur de fichiers Telegram ajouté au bot VPS** (`vps-tgbot.py`) : photos/documents sauvegardés dans `/home/ubuntu/ORDRES-files/` + entrée automatique dans ORDRES.md (confirmation envoyée). Les captures envoyées avant la mise à jour sont perdues (bot les ignorait silencieusement).
- **Nouveau repo privé `git-ops-journal`** (demande utilisateur) : journal de gestion du compte GitHub + inventaire des automatisations. Créé via webhook « Push GitHub » (PAT en variable shell, jamais affiché). Contenu : README (objectif, règles d'anonymat), `docs/regles-gestion.md` (typologie dépôts, cadences N1 7 j/N2 14 j, politique PAT, hygiène), `docs/automatisations.md` (Push GitHub, descriptions auto + deps/PR, radar cadence, rapport hebdo — avec pièges documentés), `semaines/` (rapports hebdo).
- **Rapport hebdomadaire automatique** : `/usr/local/bin/github-weekly-report.sh` (root 700) + `github-weekly.timer` **vendredi 19h00 Europe/Paris**. Collecte API `/users/<login>/events` (⚠️ `/user/events` = 404 avec PAT fine-grained), 7 jours : commits par dépôt, PR (titres enrichis par fetch — le flux events renvoie les PR tronquées), branches, issues, différentiel dépôts nouveaux/archivés (état `/var/lib/github-weekly/repos-state.json`) → `semaines/AAAA-Sww.md` → commit+push auto (token uniquement en argument du push) → récap Telegram. **Testé en réel (2 runs)** : rapport S38 publié avec les 4 PR du 17/09. Anonymat vérifié (grep : aucun secret/identifiant, seuls les motifs de vérification sont cités dans la doc).
- Bot redémarré et vert ; timer actif (prochain run vendredi 18/09 17:00 UTC = 19:00 Paris).
## Session — 2026-09-17 (2e partie)
- **Workflows Stripe et Tally invisibles → corrigé** : ils existaient en base mais sans ligne dans `shared_workflow` (rattachement projet n8n) → ajoutés au projet. Visibles dans l'UI, l'utilisateur a saisi les clés et activé les deux workflows.
- **Signature Stripe opérationnelle** : `require('crypto')` était bloqué dans le nœud Code (task runner n8n 2.x) — la bonne variable est **`NODE_FUNCTION_ALLOW_BUILTIN=crypto`** dans le compose (`NODES_ALLOW_BUILTIN` ne marche PAS pour les runners). Testé : payload signé valide → aucune ligne d'alerte ; signature falsifiée → 🚨 Telegram. Vérification multi-candidats (corps brut + re-sérialisé). Test réel avec montant 0 (demande utilisateur) : alerte reçue.
- **Signature Tally câblée** (demande utilisateur) : en-tête `Tally-Signature`, HMAC-SHA256 sur `<t>.<payload>`, hexa ET base64 acceptés, secret dans `CONFIG.webhookSecret` (vide pour l'instant — l'utilisateur doit coller la clé Tally). `LIEN_CLIENT` déjà rempli par l'utilisateur. Test réel : alerte reçue avec aperçu des champs.
- **Piège DB** : quand on insère le snapshot `workflow_history` avec `SELECT ... FROM workflow_entity`, il faut le faire APRÈS (ou avec les nouveaux nodes inline) sinon le snapshot prend les anciens nodes et l'exécution continue sur l'ancien code malgré `activeVersionId` à jour (2 tests ratés à cause de ça).
- **Tally : signature opérationnelle** (testée : payload signé valide → aucune alerte 🚨 ; signature falsifiée → 🚨 Telegram). ⚠️ La clé de signature Tally n'a PAS le format Stripe : elle commence par `tly-` (pas `whsec_`) — mon premier script de test cherchait `whsec_` et signait avec un secret vide → faux « INVALIDE ». Bug corrigé au passage dans le code : `Digest already called` (objet HMAC réutilisé → 2 objets séparés). La clé a été collée par l'utilisateur dans `CONFIG.webhookSecret`.
## Session — 2026-09-17 (3e partie)
- **Nouveau workflow « Tally — alerte photographe Telegram »** (demande utilisateur) : formulaire Tally « Contact photographe - Emmanuel Ruaudel » (photographe de Rennesdev.fr). Webhook POST `/webhook/tally-photographe` → Code (message 📸 « Pour : le photographe de Rennesdev.fr », aperçu des champs, vérification de signature Tally câblée, `LIEN_CLIENT` placeholder vide) → Telegram (même chat). **Actif et testé** (alerte reçue). Reste utilisateur : webhook Tally du formulaire photographe → URL ci-dessus, coller la clé de signature (`tly-...`) de CE webhook, plus tard le `LIEN_CLIENT`.
- **GitHub : nouveau token déployé partout + 3 PR créées 🎉** : l'utilisateur a généré un token fine-grained neuf (les 2 premiers collage étaient ratés : 1616 car. base64 puis un SHA-1 de 40 car. — vérifier que la chaîne commence par `github_pat_` ou `ghp_`). Token collé dans « Push GitHub » → nœud « Config (PAT GitHub) », puis propagé par l'assistant vers « GitHub — descriptions auto » (1 occurrence) et « Agent Telegram » (1 occurrence) via SQL, jamais affiché. **Test réel : run #271 → 3 PR ouvertes** (Fiscale-vps PR #3, surveillance-tarifaire, construction-site-tracker PR #1) sur les branches `deps/maj-auto-2026-09-17`. Fix cosmétique au passage (compteur « Branches sans PR » négatif). Cron restauré lundi 22h00.
- ⚠️ **Ancien token `...kSviHL3I` à révoquer** sur GitHub (il a transité en clair dans d'anciennes sessions et n'est plus utilisé nulle part).
- 2 pièges au passage : (1) la variable d'env d'un heredoc ne passe pas au python qui écrit le SQL si on la préfixe sur une autre commande ; (2) modifier le cron depuis un fichier local périmé peut réécrire l'ANCIEN token en base — toujours recharger l'état frais depuis la DB avant toute écriture.
- Note utilisateur : le formulaire Tally est pour le **développeur** de Rennesdev.fr ; une **autre alerte à créer plus tard pour le photographe** de rennesdev.fr.
- **GitHub PR toujours en échec** : le PAT stocké dans n8n (l'unique, empreinte `...kSviHL3I`, partagé par deps/Push/Agent Telegram) refuse encore POST /pulls (403) alors que l'utilisateur dit avoir mis à jour les permissions (mail de confirmation reçu). Le token mis à jour n'est probablement pas celui utilisé par n8n — en attente de vérification utilisateur.

- **GitHub PR : token incohérent confirmé par diagnostics** — l'utilisateur n'a qu'UN token fine-grained (empreinte `...kSviHL3I`) avec « Pull requests: Read and write » visible dans l'UI, mais l'API donne : créer une issue ✅, la modifier ❌ 403, créer une PR ❌ 403, lister les PR ✅. Le token est dans un état incohérent → **décision : créer un token neuf** et le remplacer dans n8n (utilisateur colle le nouveau dans « Push GitHub » → nœud « Config (PAT GitHub) », l'assistant le propage ensuite aux nœuds Dépendances + Agent Telegram sans l'afficher). Une issue de test (#1) a été créée sur Fiscale-vps pendant le diagnostic et n'a pas pu être fermée via l'API (403) — à fermer à la main.

## Session — 2026-09-17
- **Dépendances GitHub → PR automatiques + cron lundi 22h00 (ordre utilisateur)** : workflow « GitHub — descriptions auto » modifié — nœud hebdo renommé « Horloge hebdo (lun 22h00) » (cron `0 22 * * 1`), nœud Code « Dépendances — vérifie et crée les PR » : après création de la branche `deps/maj-auto-<date>`, **ouverture automatique d'une PR** branche → défaut (titre + corps avec liste à cocher des maj, URL dans le récap Telegram 🌿). Si la branche existe déjà sans PR → la PR est ouverte au run suivant. **Testé en réel** : runs #248/#249, branches 2026-09-17 créées mais **PR en échec 403** = PAT fine-grained sans permission « Pull requests: write » (⚠️ action utilisateur : ajouter la permission, les PR partiront au run suivant). 2 bugs corrigés en route : GET de vérification de branche avait été écrasé par la modif (message « déjà ouverte » à tort), double comptage stats. Export + README mis à jour, poussés GitHub.
- **Workflow « Stripe — alerte paiement Telegram » créé (ordre utilisateur)** — désactivé, en attente des clés : Webhook POST `/webhook/stripe-paiement` → Code (filtre sur checkout.session.completed / payment_intent.succeeded / invoice.payment_succeeded / charge.succeeded, formatage montant/devise/client, **vérif optionnelle de la signature Stripe** HMAC-SHA256 si `CONFIG.webhookSecret` renseigné, sinon ⚠️ dans le message) → Telegram (credential « Telegram account » existant, chat 8634051625). À faire par l'utilisateur : endpoint webhook dans le dashboard Stripe + clé `whsec_...` + activation.
- **Workflow « Tally — alerte message Telegram » créé (ordre utilisateur)** — désactivé : Webhook POST `/webhook/tally-alerte` → Code (alerte simple : nom du formulaire, id réponse, aperçu des champs ; `CONFIG.LIEN_CLIENT` placeholder pour le lien futur) → Telegram (même chat). À faire : ajouter l'URL dans Tally (Settings → Webhooks) + activer.
- Exports + README (github-descriptions-auto, stripe-alerte-paiement, tally-alerte-message) poussés sur GitHub (PAT masqué, vérifié). Tout vert au contrôle santé (exit 0).

## Session — 2026-09-16 (2e partie)
- **Chaîne hebdo « Dépendances » ajoutée au workflow « GitHub — descriptions auto »** (demande utilisateur) : 2e déclencheur `Horloge hebdo (dim 08h30)` (cron `30 8 * * 0` Paris) → nœud Code « Dépendances — vérifie et crée les branches » → Telegram. Pour chaque repo actif : détection `package.json`/`requirements.txt` (tree API), comparaison aux dernières versions stables npm/PyPI (préversions ignorées), puis **création d'une branche `deps/maj-auto-<date>`** avec 1 commit de maj (API Git : blob→tree→commit→ref, `main` jamais touché, préfixes `^`/`~` préservés). Branche déjà ouverte = rien de créé. Récap Telegram : 🌿/✅/➖/↩️/⚠️ par repo. **Testé en réel** : 3 branches créées (Fiscale-vps, surveillance-tarifaire, construction-site-tracker — Capacitor 6→8), diffs vérifiés OK. **2 bugs corrigés en route** : (1) `httpRequest({json:false})` auto-parse les JSON → `package.json` arrive en objet, `JSON.parse('[object Object]')` échouait silencieusement (fix `getTexte()`) ; (2) `encodeURIComponent` sur un package npm scopé casse l'URL du registre (URL sans encodage). Divers : deps non épinglées signalées, récap honnête. Débogage facilité par un mini-workflow webhook temporaire « debug-npm » (créé puis supprimé). ⚠️ Suppression d'un workflow en base 2.39 : `workflow_entity.versionId` FK NOT NULL vers `workflow_history` — supprimer l'entité en premier (cascade). Export + README (section « Dépendances ») poussés sur GitHub.

## Session — 2026-09-16 (matin)
- **Quota Airtable (ordre utilisateur)** : forfait gratuit épuisé. Cause : les 2 workflows « Diffusion » faisaient 1 appel de dédup PAR article + 1 appel de marquage PAR article + 1 appel liste des flux, toutes les 30 min (~1 500-3 000 appels/mois). **Restructurés** : dédup groupée (1 appel, formule OR sur ≤ 20 liens < 48 h), marquage groupé AVANT envoi Discord (1 appel, ≤ 10 records → jamais de doublon Discord si le quota lâche), liste des flux en cache staticData 12 h, cron 3×/jour (Actus 09:00/15:00/21:00, Photos 09:30/15:30/21:30 Paris), **pause auto 24 h sur 429** (staticData). Coût cible ~6-10 appels/j (~300-400/mois). **3 pièges DB n8n 2.39 rencontrés** : (1) éditer `workflow_entity` ne suffit plus (draft/publish) — insérer un snapshot dans `workflow_history` + pointer `workflow_entity."activeVersionId"` dessus ; (2) les `connections` référencent les nœuds **par nom** — renommer un nœud sans maj des connexions coupe le fil (runs 229/230 « success » en 0 s sans rien faire !) ; (3) `psql` sans `-v ON_ERROR_STOP=1` réussit « en silence ». Corrigé, testé (#231/#232 : 6-7 s, 1-2 appels Airtable au lieu de ~100), crons finaux restaurés. ⚠️ Quota du mois en cours déjà épuisé → runs « skipped quota » jusqu'au reset mensuel.
- **Workflow « GitHub — descriptions auto » repris et terminé (ordre utilisateur)** : complet mais jamais exécuté. **2 bugs trouvés et corrigés** : (1) le nœud « Nettoie la description » utilisait `$('Filtre…').item` en mode all-items → pairing résolu vers le même item pour tous → tous les PATCH partaient sur 1 seul dépôt (statut n8n « success » quand même !) ; fix = mode `runOnceForEachItem` (dans `parameters.mode`, pas au niveau du nœud — 1re tentative ratée pour ça). (2) cron de test 21:07 remplacé par 07:45 Paris (conforme au nom du nœud). **Tests bout-en-bout** : #223 (11 générations → 1 PATCH sur rennesdev, description « Portfolio en HTML » ajoutée — rien détruit, le repo n'avait pas de description), #224 (10 générations → 1 PATCH, local_contextual_ai décrit), **#225 OK : les 12 repos actifs ont tous une description** (style conforme aux exemples du prompt). Récap Telegram envoyé à chaque run (bot Bot-vps).
- **n8n mis à jour 2.38.7 → 2.39.6** (ordre utilisateur) : pg_dump de secours avant (`~/n8n-db-avant-MAJ-20260916.sql.gz`), `docker compose pull && up -d`, migrations OK, HTTP 200.
- **Workflows exportés + push GitHub** : exports à jour (actus, photos, github-descriptions-auto — nouveau dossier avec README documentant le bug de pairing), secrets masqués (double vérification), push via webhook « Push GitHub » OK.
- ⚠️ Rappels : token Airtable + webhook Discord + PAT GitHub visibles en clair dans cette session pendant les inspections DB (rotation possible sur demande).

## Session — 2026-09-15
- **Watchdog corrigé (demande utilisateur)** : les alertes « Dépôt Kopia NON CONNECTÉ » ne partent plus quand le PC est simplement éteint (situation normale). Le watchdog teste désormais la joignabilité du serveur Kopia du PC (100.118.76.30:51515) AVANT tout check Kopia : alerte uniquement si PC allumé mais dépôt déconnecté, ou PC allumé + snapshot > 9 j. Motif : 5 alertes en 3 jours (13-15/09) uniquement dues au PC fermé.
- **Inventaire GitHub fait (demande utilisateur)** : 97 dépôts listés via API (PAT Agent Telegram extrait en variable shell). Top 10 des projets dormants à garder actifs + proposition de cadence de maintenance (3 niveaux) remise à l'utilisateur. Hygiène : doublons détectés (Fiscale-vps/Fisclale-vps/Fiscale_vps, Devis-Python/Devis-Python-Steamlit). ⚠️ PAT Agent Telegram affiché en clair dans la session pendant l'inspection du nœud — rotation possible sur demande.
- **Archivage GitHub massif (demande utilisateur)** : 79 dépôts archivés via API (PATCH), **18 conservés** = 12 nommés (actifs + Top 10 dormants) + 6 actifs récents (test-architecture-openclaw, test-workflow-ollama-openclaw, Ai-agent-prosgres, Session-conseil-stripe, Booking-Conseil---Stripe, ia-nano-google-local — archiver aussi si demandé).
- **Radar de cadence GitHub implémenté (point 4, demande utilisateur)** : workflow « Agent Telegram » enrichi (nœud « Prépare le résumé ») — fenêtres de commit codées dans `NIVEAU1` (rennesdev, rennesdev-vps-ops, RuaudelEmmanuel.github.io, RuaudelPhoto = 7 j) et `NIVEAU2` (Rennesdev-api, Fiscale-vps, Trombi, construction-site-tracker, Besoin-visio, local_contextual_ai, ai_gemma_service, surveillance-tarifaire = 14 j), section « Cadence — dépassements » avec lignes ⚠️, GitHub node passé à per_page=100, systemMessage ajoute « ⏳ À committer » + interdiction du chinois + « ne se baser que sur le rapport courant », mémoire chat `github-daily` purgée (8 lignes). Cron restauré **0 7 * * * Paris**. ⚠️ Piètre corrigée : le champ cron n8n s'appelle `expression` (pas `cronExpression`) — 2 tests ratés avant de le trouver. **Test bout-en-bout validé** (exécution #157, 21:24 Paris, success : 18 actifs/79 archivés, ⏳ À committer + 💡 Conseils, français uniquement). Doc `docs/cadence-github.md` + export workflow (PAT masqué) poussés sur GitHub.

## Session — 2026-09-14
- **Netdata installé** (supervision temps réel, choisi face à Prometheus) : conteneur `netdata`, ~200 Mo RAM, UI 127.0.0.1:19999 (accès = tunnel SSH), métriques VPS + conteneurs. Docs : `rennesdev-vps-ops/docs/netdata.md`.
- **Rapport quotidien Telegram 08:00 Paris** créé (`vps-metrics-report.timer` → `/usr/local/bin/vps-metrics-report.sh`) : uptime, RAM, disque, conteneurs, HTTPS, fail2ban, Kopia+PC, ollama, netdata. Testé OK.
- **Skill `vps-sante` créé** (`~/.agents/skills/vps-sante/`) : contrôle de santé standardisé, script exit 0/1, tout vert au test.
- **Rotation du journal** : historique → `ETAT-VPS-archive-2026-09.md`, ce fichier réécrit (5,5 Ko vs 54 Ko).
- **Méthode push GitHub validée** : PAT extrait de la base n8n en variable shell, jamais en session (doc `docs/push-github.md`).
- **Workflow « Diffusion Actus Discord » créé (demande utilisateur)** : RSS (Airtable) → Discord. Airtable `Base RSS Manu` (`appzFRnAeQHJENm11`) : flux dans « Table 1 » (Name, URL source rss, Salon discord = webhook), marquage dans « Table RSS articles » (clé unique Lien). 2 nœuds : Horloge `*/30 * * * *` Paris + Code (parse RSS/Atom, dédup par filterByFormula, embed Discord, anti-flood 5/flux/run, rate-limit 250-400ms). Secrets (token Airtable + webhook Discord) dans le code n8n uniquement. Débogage : param `fields=` en query Airtable = 422 (retiré), redirections n8n OK. SharedWorkflow (projet personnel) requis pour l'activation. **Test réel validé** : exécution id 42, 20 articles envoyés + marqués (5/flux, premier passage). ⚠️ Token Airtable et webhook Discord ont transité en clair dans la conversation — rotation possible sur demande. **✅ Rotation FAITE par l'utilisateur (14/09 soir)** : nouvelles clés saisies via l'éditeur n8n (18:01 Paris, publiées directement — draft=active, vérifié par empreintes). Validations : token Airtable OK (meta/bases), webhook Discord OK (2 messages de test reçus, 204), champ « Salon discord » des 4 flux Table 1 mis à jour vers le nouveau webhook via API, exécution #49 (20:30 Paris) success avec les nouvelles clés. L'exécution #48 (20:00, 1 min avant la publication) a échoué sur l'ancien token révoqué — transition attendue, rien à faire.
- **Images dans les embeds Discord (demande utilisateur)** : extraction de l'image par article
  (media:content/thumbnail, enclosure, 1re img du contenu) + repli og:image/twitter:image sur la
  page de l'article si le flux n'en fournit pas. Testé localement sur les 4 flux (couverture 100 %),
  déployé, exécution #44 (17:00 UTC) success. Export + README mis à jour, poussé. **✅ Confirmé par l'utilisateur** : premiers articles illustrés bien reçus sur Discord.
- **Workflow « Diffusion Photos Discord » créé — PHASE 1 (demande utilisateur)** : clone de Diffusion Actus pour la base Airtable « rss photo », embeds violets, salon par flux + webhook par défaut, mapping des champs centralisé dans CONFIG (champsFlux/champsPhotos). Créé **désactivé** avec placeholders (`AIRTABLE_TOKEN/BASE_ID/FEEDS_TABLE_ID/PHOTOS_TABLE_ID/DISCORD_WEBHOOK_PLACEHOLDER`) — id `0ciq8j8DYSBXewMECOnp0`. Le token Airtable actuel ne voit pas la base « rss photo » (à rattacher au token ou nouveau token). Phase 2 : l'utilisateur remplace les placeholders + active, l'assistant teste.
- **Workflow « Diffusion Photos Discord » — PHASE 2 TERMINÉE, en production** : utilisateur a remplacé les placeholders (noms lisibles → assistant a découvert les vrais IDs via le token : base `appLn9VCOlsdLdp1U`, flux `tblHHzzA0AR8tHlZi`, champ url = `rss`, pas de salon par flux → webhook par défaut). Table de marquage « Photos envoyées » créée par l'assistant via API après ajout du scope `schema.bases:write` au token (7 champs dont Image). Débogages : SyntaxError (virgule avalée par commentaire — corrigé, node --check systématique désormais), décodage entités numériques HTML ajouté, champ Image écrit dans Airtable. **Test réel : exécution #55 success, 15 photos envoyées sur Discord et marquées** (5/flux, anti-flood premier passage). Cron `*/30 * * * *` restauré. 💡 Reste cosmétique côté utilisateur : remplir la colonne `Name` des 4 flux (vide → « flux sans nom » dans les footers Discord). ✅ **Fait le 14/09 soir** (phototrend, argentique.net, Dans la cuve, benber) — vérifié via API, effectif au prochain nouvel article.
- Tout est vert (skill vps-sante exit 0).
- **Workflow « Agent Telegram » enrichi (demande utilisateur)** : 2e déclencheur **Horloge 07:00 Europe/Paris** → HTTP GitHub (`/user/repos`, PAT fine-grained injecté dans le nœud via extraction DB, jamais en session) → Code « Prépare le résumé » → même AI Agent (prompt `{{ $json.message ? $json.message.text : $json.resume }}`, mémoire sessionKey fallback `'github-daily'`) → même nœud d'envoi Telegram. Édité en base (entity + history, timezone Europe/Paris ajoutée aux settings), n8n_workflow redémarré. **Test bout-en-bout réussi** : cron temporaire 16:47 Paris → exécution mode trigger, succès (id 38). Cron définitif `0 7 * * *` appliqué + réactivation vérifiée. Export repo mis à jour (PAT → placeholder, vérification aucun secret). **Phase 1 « conseilleur » appliquée** : le code de résumé calcule désormais les signaux (description manquante, repos dormants > 60 j) et le systemMessage impose une section « 💡 Conseils » (2-3 max, actionnable). **Test bout-en-bout validé** (exécution id 40, success, 17:10 Paris). Phases 2 et 3 refusées par l'utilisateur (pour l'instant).
- **Vhost Caddy netdata.rennesdev.fr + basic auth créé (2e session)** : mot de passe fort généré (24 car., bcrypt via `caddy hash-password`), credentials dans `/etc/caddy/netdata-auth.txt` (root 600, inclus dans le snapshot /etc/caddy). Config validée + reload OK. DNS créé par l'utilisateur (record A proxie orange) → **cert Let's Encrypt émis automatiquement par Caddy** (expire 2026-12-13, renouvellement auto). **Validation complète** : origine 401 sans auth / 200 avec auth ; edge 401 / 200. Dashboard accessible sur https://netdata.rennesdev.fr (identifiants dans le fichier root-only).

---

## Infrastructure
- VPS OVH — Ubuntu 26.04 LTS, 4 vCPU, 7.6 Go RAM, 72 Go disque (~30-31 Go utilisés, 42-44%)
- IPv4 : 162.19.246.165 — IPv6 : 2001:41d0:701:1100::208c — hostname Tailscale : `vps-5532a57a` (100.75.226.13)
- DNS chez **Cloudflare** (proxy orange, Full strict, Always Use HTTPS)

## Services & stacks
| Élément | Détail |
|---|---|
| Frontal | **Caddy** (80/443, certs ACME auto, expirent 2026-12-10, renouvellement auto) — `/etc/caddy/Caddyfile` |
| Umami | `umami_app` (127.0.0.1:3000) + `umami_db` (PG 16) — stack `~/umami/`, creds `~/umami/.credentials.txt` |
| n8n | **v2.39.6** (maj 16/09) — `n8n_workflow` (127.0.0.1:5678) + `n8n_db` (**PostgreSQL 17**) — `~/docker-compose.yml`, mdp dans `~/.env` (`N8N_DB_PASSWORD`) |
| Ollama | `ollama` (127.0.0.1:11434), réseau Docker `apps` — modèles : `qwen2.5:7b` (bot IA), `llama3.2:3b`, `qwen2.5:3b`. `OLLAMA_KEEP_ALIVE=30m` |
| Netdata | `netdata`, UI **127.0.0.1:19999** + **https://netdata.rennesdev.fr** (basic auth, credentials `/etc/caddy/netdata-auth.txt` root 600) — `~/netdata/docker-compose.yml` |
| Bot contrôle | `vps-tgbot.service` → `/usr/local/bin/vps-tgbot.py` (long polling) : `/backup`, `/status`, `/ordres`, `/ok`, `/help` |
| Bot IA | Workflow n8n « Agent Telegram » (bot dédié) : Telegram → Filter chat.id → Ollama qwen2.5:7b + PG Chat Memory → réponse |
| Sécurité | UFW (22/80/443), fail2ban (sshd + recidive), SSH par clé uniquement, `passwordauthentication=no` |

## Timers systemd
| Timer | Horaires (Europe/Paris) | Rôle |
|---|---|---|
| `vps-watchdog` | toutes les 15 min | anomalies → Telegram (`/usr/local/bin/vps-watchdog.sh`) |
| `vps-metrics-report` | tous les jours 08:00 | rapport métriques → Telegram (`/usr/local/bin/vps-metrics-report.sh`) |
| `vps-ollama-unload` | tous les jours 19:15 | décharge les modèles ollama avant backup |
| `vps-backup` | dimanche 19:30 | backup Kopia complet (PC allumé requis) |
| `vps-backup-reminder` | dimanche 12:00 | rappel Telegram (PC joignable ? dépôt ? âge snapshot) |
| `github-weekly` | vendredi 19:00 | rapport hebdo GitHub → repo `git-ops-journal` + Telegram |

## Backups (Kopia)
- Chaîne : `vps-backup.timer` → `kopia-backup.sh` (staging = dump umami + pg_dump n8n + configs, puis snapshots : `/var/lib/docker/volumes`, `/home/ubuntu`, `/etc/caddy`) → **dépôt Kopia sur le PC Windows** (`super-pc-vert`, 100.118.76.30, port 51515, TLS cert RSA fixe, fingerprint `46d81925…`).
- À la demande : `/backup` Telegram → `vps-backup-on-demand.sh` (démarre le serveur Kopia du PC via SSH si besoin, clé `/root/.ssh/id_ed25519_pc`).
- Restauration : `kopia restore` (testée sur dumps). Certificat PC valable jusqu'en 2036.

## Tailscale
- VPS `vps-5532a57a` = 100.75.226.13 ↔ PC `super-pc-vert` = 100.118.76.30 (connexion directe, ~20 ms).

## 🧠 PRÉFÉRENCES UTILISATEUR (mémoire permanente)
9. **Pas d'alerte Telegram pour les situations normales** : PC éteint = pas d'alerte Kopia (le rapport 08:00 suffit comme info). Une alerte = quelque chose à faire.
1. **Canal d'alerte = Telegram** : bot `@Vosmanubot` (Bot-vps), chat `TG_CHAT_ID=8634051625`, config `/etc/vps-watchdog-telegram.env` (root 600). JAMAIS de token dans un fichier commité ou ce journal.
2. **Chaque nouveau workflow/création → repo GitHub avec README** : `github.com/Ruaudel-Emmanuel/rennesdev-vps-ops` (privé, local : `~/projects/rennesdev-vps-ops`). **Push sans PAT en session** : webhook n8n `POST https://n8n.rennesdev.fr/webhook/github-push` — PAT extrait de la base n8n (`n8n_db`, table `workflow_entity`, workflow « Push GitHub ») directement en variable shell du curl, jamais affiché ni sur disque. Voir `docs/push-github.md`.
3. Heures des timers : **Europe/Paris** (le serveur est en UTC).
4. L'utilisateur est sur **Windows 11** (`super-pc-vert`) — manipulations PC = pas-à-pas clairs.
5. **Aucun secret côté assistant** ; secrets → fichiers root-only sur le VPS.
6. **Au début de CHAQUE session : lire `/home/ubuntu/ORDRES.md`** (ordres via bot `@Vosmanubot`, message libre = ordre), les traiter, marquer ✅. `/ordres` liste, `/ok` vide.
7. **Maintenance du journal** : `ETAT-VPS.md` = état courant + dernière session ; le 1er de chaque mois → déplacer les entrées datées du mois écoulé dans `ETAT-VPS-archive-<AAAA-MM>.md` puis pusher les deux sur GitHub. Skill `vps-sante` (`~/.agents/skills/vps-sante/`) = procédure standard de contrôle de santé.
8. **Rapport quotidien 08:00 Paris** sur Telegram (`vps-metrics-report.timer`) — vérifier dans le contrôle santé qu'il est actif.
10. **Airtable forfait gratuit = 1 000 appels API/mois** (partagés entre les workflows) : tout workflow Airtable doit être économe — appels groupés (formule OR, batch records), cache staticData des listes, fréquence limitée, pause auto sur 429. Se référer au design des workflows « Diffusion » (16/09).

## ⏳ En attente (actions utilisateur)
- ✅ 2026-09-17 : points GitHub précédents tous traités (ancien token révoqué, issue fermée, PR relues, Tally photographe OK, PAT roté, dépôts confirmés) — validé par l'utilisateur.

## 🔎 Vérifications rapides utiles
```bash
bash ~/.agents/skills/vps-sante/scripts/vps-sante.sh   # contrôle complet (exit 0 = vert)
docker ps                                              # 6 conteneurs attendus
curl -s -o /dev/null -w '%{http_code}' https://n8n.rennesdev.fr   # 200
sudo fail2ban-client status sshd
docker exec ollama ollama list
curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:19999     # 200 (netdata)
systemctl list-timers github-weekly.timer --no-pager             # vendredi 19h00 Paris
```

## Notes
- Historique détaillé (crises résolues, installations, debugging) : `ETAT-VPS-archive-2026-09.md`.
- Docs ops + workflows + scripts versionnés : `~/projects/rennesdev-vps-ops/` (= repo GitHub).
- Skill de contrôle santé : `~/.agents/skills/vps-sante/` (copié dans le repo GitHub).
